import React from 'react';

import '../../../../index.css'

const toolbar = (props) => {
    return (
        <header className="Toolbar">
            <div>MENU</div>
            <div>LOGO</div>
            <nav>
                ...
            </nav>
        </header>
    );
};

export default toolbar;
