/** first we looked at BurgerBuilder.js to see what methods we use there (it's addIngredientHandler and removeIngredientHandler) and then we start to create actions named like this*/

export const ADD_INGREDIENT = 'ADD_INGREDIENT';
export const REMOVE_INGREDIENT = 'REMOVE_INGREDIENT';